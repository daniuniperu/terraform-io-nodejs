{
  "name": "validate_user",
  "version": "1.0.0",
  "description": "Lambda function to validate a user",
  "main": "src/main.ts",
  "scripts": {
    "build": "tsc",
    "test": "mocha 'src/**/*.test.ts'",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "@nestjs/common": "^10.0.0",
    "@nestjs/core": "^10.0.0",
    "aws-sdk": "^2.1370.0"
  },
  "devDependencies": {
    "@types/node": "^20.2.5",
    "typescript": "^5.1.6"
  }
}
